[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: requiere API levantada en localhost (no ejecutar en CI por defecto)
    chat: llama a Gemini (costoso; excluido por defecto)
addopts = -m "not integration and not chat"
